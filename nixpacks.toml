[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "zlib", "libjpeg"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
